appVersion=1.0.0
kotlin.code.style=official

# Kotlin/Native release linking needs more headroom than the 512 MiB default; without this the
# daemon can be killed mid-link.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.caching=true
org.gradle.parallel=true
